using System.Threading.Tasks;
using ff14bot;
using Magitek.Extensions;
using Magitek.Models.Roles;
using Magitek.Utilities;

namespace Magitek.Logic.Roles
{
    internal static class MagicDps
    {
        // public static async Task<bool> Drain<T>(T settings) where T : MagicDpsSettings
        // {
        //     if (!settings.UseDrain)
        //         return false;
        // 
        //     if (Core.Me.CurrentHealthPercent > settings.DrainMinimumHealthPercent)
        //         return false;
        // 
        //     return await Spells.Drain.Cast(Core.Me.CurrentTarget);
        // }
    }
}
